spring:
  application:
    name: auth-service


server:
  port: 9099
  servlet:
    context-path: /


eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/
  instance:
    hostname: localhost
    port: 8761

